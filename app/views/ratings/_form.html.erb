<%= form_for(@rating) do |f| %>

  <%=shows_error_messages(@rating)%><br>

  <%= id_field(@rating,@pairing) %>
  <%= f.label :rating_value %>
  <%= f.select :rating_value, ['1', '2', '3','4','5']%>

  <%= f.submit %>
<% end %>
