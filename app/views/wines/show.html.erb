

<%= render 'wineinfo', wine: @wine %>

<% if user_admin? %>
  <%= link_to 'Edit Wine', edit_wine_path(@wine)%><br>
  <%= link_to "Delete", @wine, method: :delete, data: {confirm: "Really?!"} %>
<% end %>

<%= render 'cookie_pairings', wine: @wine %>

<%= render 'create_pairing', wine: @wine %>


<%= link_to 'Add a New Wine', new_wine_path%><br>
<%= link_to 'All Cookies', cookies_path%><br>
<%= link_to 'All Wines', wines_path%><br>
<%= link_to 'All Pairings', pairings_path%><br>
<%= link_to 'Home', home_url%>
